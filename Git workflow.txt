Need to generate personal access token from Settings > Developer Settings in github and use personal access token as passwordNavigate to directory of bookThen type git add --allgit commit -a -m “update”git push -uAfter running git push -u, enter the relevant username and token as the password if it is the first time using a new personal access token.You will find the book at https://daarnolducsd.github.io/ep5book/